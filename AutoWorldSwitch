-- AutoWorldSwitch.lua  ·  AUTOEXECUTE FILE
-- Place this in your executor's Autoexecute folder. Never change it.
-- MFRotation_Start.lua installs the full script as MFRotFarm.lua
-- in the workspace root. This loader runs it on every server start.
if isfile and isfile("MFRotFarm.lua") then
    _G._MFRotAutoLoaded = true   -- tells the script it was loaded by autoexec
    loadstring(readfile("MFRotFarm.lua"))()
else
    print("[MFRot] MFRotFarm.lua not found. Run MFRotation_Start.lua once to install it.")
end
